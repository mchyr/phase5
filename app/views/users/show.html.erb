<div id="container">
    <br>

<p class="supertiny"></p>

<table>
	<thead>
		<th>Use Details</th>
	</thead>

	<tr>
		<td><b>Employee:</b>
		<%= link_to @user.employee.proper_name, employee_path(@user.employee) %></td>
	</tr>

	<tr>
		<td><b>Email:</b>
		<%= @user.email %></td>
	</tr>

	<tr>
		<td><b>Role:</b>
		<%= @user.employee.role.capitalize %></td>
	</tr>

</table>

<%= link_to 'Edit', edit_user_path(@user), :class => "btn" %>
<%= link_to 'Back', users_path, :class => "btn" %>